name: Clear AI Cache

on:
  workflow_dispatch: {}

permissions:
  actions: write

jobs:
  clear-cache:
    runs-on: ubuntu-latest
    steps:
      - name: Clear cache entries
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          echo "Listing data cache entries..."
          gh cache list --repo "$REPO" --key data-cache- --json id,key | \
            jq -r '.[].key' | while read key; do
              echo "Deleting cache: $key"
              gh cache delete "$key" --repo "$REPO"
            done
          echo "Cache cleared. Next deploy will re-fetch all data and re-analyze."
